{
  "name": "ai-image-tool-suite",
  "version": "1.0.0",
  "private": true,
  "description": "A modern web application featuring a suite of AI-powered image tools.",
  "scripts": {
    "build": "tsc && mkdir -p dist && cp -r components constants hooks services *.html *.css *.js *.json dist"
  },
  "dependencies": {
    "@google/genai": "^1.25.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
